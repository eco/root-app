import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // We'll use SPA mode for development
  // For production, uncomment the line below to enable static export
  // output: 'export',
};

export default nextConfig;
